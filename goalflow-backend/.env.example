# --- Supabase Postgres (project: sgzboygiqtegxuhhdhmz, region: ap-northeast-2) ---
# Paste your DB password where [YOUR-DB-PASSWORD] appears.
# Find it: Supabase Dashboard → Project Settings → Database → "Database password" (reset if you don't remember it).
#
# DATABASE_URL  = transaction pooler (port 6543) — used by the running app
# DIRECT_URL    = session pooler / direct (port 5432) — used by Prisma migrations
DATABASE_URL="postgresql://postgres.sgzboygiqtegxuhhdhmz:[YOUR-DB-PASSWORD]@aws-0-ap-northeast-2.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1"
DIRECT_URL="postgresql://postgres.sgzboygiqtegxuhhdhmz:[YOUR-DB-PASSWORD]@aws-0-ap-northeast-2.pooler.supabase.com:5432/postgres"

# Supabase Auth (used to validate Entra ID SSO tokens — Phase 2.A real SSO)
SUPABASE_URL="https://sgzboygiqtegxuhhdhmz.supabase.co"
SUPABASE_PUBLISHABLE_KEY="sb_publishable_mekni0RsCLI5S276IuxP3g_ypbDAgRI"

# Frontend URL — used to build deep-links in emails / Teams cards
APP_URL="http://localhost:8081"

JWT_SECRET="change-me-in-production"
PORT=3001
NODE_ENV=development
CORS_ORIGIN=http://localhost:8080,http://localhost:8081,http://localhost:5173,http://localhost:3000

# --- Phase 2 bonus integrations ---
# Email via Resend (free tier: 3000/mo, 100/day). Leave blank for console fallback.
RESEND_API_KEY=
EMAIL_FROM="GoalFlow <onboarding@resend.dev>"

# Microsoft Teams incoming webhook. Leave blank for console fallback.
TEAMS_WEBHOOK_URL=

# Email (optional — leave blank for console-only mode in dev)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@goalflow.atomberg.local

# Microsoft Teams incoming webhook (optional)
TEAMS_WEBHOOK_URL=

# Mock Microsoft Entra ID issuer (demo only)
ENTRA_TENANT=atomberg.onmicrosoft.com
